{
    "name": "ytarr",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "ui:dev": "cd ui && pnpm dev",
        "ui:build": "cd ui && pnpm install && pnpm build",
        "ui:start": "cd ui && pnpm start",
        "ui:lint": "cd ui && pnpm lint",
        "cron:dev": "cd cron && pnpm dev",
        "cron:build": "cd cron && pnpm install && pnpm build",
        "cron:start": "cd cron && pnpm start",
        "db:start": "/db/pocketbase serve --http 0.0.0.0:8090"
    }
}